# TechPanda Wake Word

This directory contains the TechPanda wake-word model and sample-generation tooling. The local Python environment, generated samples, and the large upstream `microWakeWord` checkout are intentionally excluded from the public repository. Recreate them from the documented requirements and upstream project when training or converting a model.
